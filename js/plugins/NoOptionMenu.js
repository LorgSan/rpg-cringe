Scene_Map.prototype.callMenu = function() { //what this plugin does is that it "hides" original code
    // SoundManager.playOk();
    // SceneManager.push(Scene_Menu);
    // Window_MenuCommand.initCommandPosition();
    // $gameTemp.clearDestination();
    // this._mapNameWindow.hide();
    // this._waitCount = 2;
};